UPDATE screeny_inventory SET
    rabatsystem = '{{Input18.text}}',
   
  WHERE fk_oferty_inventory_id = {{appsmith.URL.queryParams.id_oferta}}; -- Specify a valid condition here. Removing the condition may update every row in the table!